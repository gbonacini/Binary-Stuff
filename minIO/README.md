Description:
============

* This concept shows how to write on stdout without using any part of standard library (no dependency, no include, no link);
* In order to do that, an inline assembly code wrap directly the write sycall;
* See "examples" directory for a demo.

